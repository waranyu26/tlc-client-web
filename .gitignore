# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# dependencies
/node_modules
/.pnp
.pnp.*

# yarn berry
/.yarn/*
!/.yarn/patches
!/.yarn/plugins
!/.yarn/releases
!/.yarn/versions

# testing
/coverage

# build outputs
/.next/
/out/
/dist
/build

# environment variables
.env*
!.env.example

# deployment
.vercel
.netlify

# typescript
*.tsbuildinfo
next-env.d.ts

# editor & system files
.DS_Store
.vscode

# local configs & certificates
*.pem
.ncurc.js

# Source audio staged for upload to the asset bucket. The app loads the track
# by URL from the bucket, never from the bundle, so the file itself has no
# reason to be in the repo — and a multi-megabyte binary in git history is
# permanent in a way the bucket object is not.
music/
